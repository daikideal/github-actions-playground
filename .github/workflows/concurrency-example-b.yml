name: concurrency-example-b
run-name: Concurrency Example B (for testing by ${{ github.actor }})
on:
  push:
    branches:
      - main
concurrency:
  group: concurrency-example
  cancel-in-progress: true
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello, world!"
